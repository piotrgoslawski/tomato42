[package]
name = "tomato42-protocol"
version = "0.1.0"
edition = "2021"
description = "Shared IPC protocol types for the tomato42 plant simulator"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
